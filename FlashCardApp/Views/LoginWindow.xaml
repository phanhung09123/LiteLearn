<Window x:Class="FlashCardApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="225" Width="300">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Username:"/>
            <TextBox x:Name="UsernameBox" Margin="0,5"/>
            <TextBlock Text="Password:"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,5"/>
            <Button Content="Login" Margin="0,10" Click="Login_Click"/>
            <Button Content="Register" Margin="0,10" Click="Register_Click"/>
        </StackPanel>
    </Grid>
</Window>
